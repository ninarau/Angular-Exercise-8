/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PoistoPipe } from './poisto.pipe';

describe('PoistoPipe', () => {
  it('create an instance', () => {
    let pipe = new PoistoPipe();
    expect(pipe).toBeTruthy();
  });
});
